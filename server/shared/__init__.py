"""Shared helpers used by multiple subsystems (Compass, Playbook, ...).

Modules here are deliberately subsystem-agnostic. If a helper has
become specific to one subsystem, it belongs back in that subsystem.
"""
