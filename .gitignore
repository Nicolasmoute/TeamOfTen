# Python
spike/.venv/
.venv/
.venv_blacklisted_*/
__pycache__/
*.pyc

# Auto-generated by scripts/bootstrap_worktree.sh in sub-worktrees —
# a thin shim that runs the main worktree's .venv against THIS
# worktree's source. Lives at repo root only.
/pytest

# Local spike runs (outputs captured during laptop testing)
spike/runs/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp
Thumbs.db

# Secrets — never commit .claude.json, .claude/, or .env
.claude.json
.claude/
.env
.env.*
!.env.example

# Retired specs — kept locally for reference, not tracked
Docs/Deprecated/
